<Page x:Class="WpfApp2.AddEditPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp2"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddEditPage">

    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="ID"/>
            <TextBox Text="{Binding ID}" Width="150" Height="30" Margin="5"/>
            <TextBlock Text="Наименование"/>
            <TextBox Text="{Binding Name}" Width="150" Height="30" Margin="5"/>
            <TextBlock Text="Цена"/>
            <TextBox Text="{Binding Cast}" Width="150" Height="30" Margin="5"/>
            <TextBlock Text="Колличество"/>
            <TextBox Text="{Binding Count}" Width="150" Height="30" Margin="5"/>
            <TextBlock Text="Поставщик"/>
            <TextBox Text="{Binding Postav}" Width="150" Height="30" Margin="5"/>
            <Button x:Name="btnSave" Content="Сохранить" Width="150" Height="30" Margin="5" Click="btnSave_Click"/>
            <Button x:Name="btnExit" Content="Назад" Width="150" Height="30" Margin="5" Click="btnExit_Click"/>
        </StackPanel>
    </Grid>
</Page>
